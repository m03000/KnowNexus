# KnowNexus 0.1.0

- 解压后直接运行 `KnowNexus.exe`，关闭窗口后驻留系统托盘。
- 随包包含独立后端、Tesseract OCR 与 Whisper small。
- Embedding 与 Reranker 模型在基础配置中按需下载。
- 用户数据保存在 `%APPDATA%\KnowNexus`，更新程序不会覆盖个人数据。
- 内置 Codex 监听首次运行默认关闭，可在设置中主动启用。

下载 ZIP 后可使用同一 Release 中的 `SHA256SUMS.txt` 验证文件完整性。
