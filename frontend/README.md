# KnowNexus Frontend

KnowNexus 的 React、Vite 与 Electron 桌面前端。完整安装、开发和构建说明请参阅项目根目录的 [README](../README.md)。

```powershell
npm ci
npm run dev
```

Vite 默认运行于 `http://localhost:3000`，并将 `/api` 代理到 `http://localhost:8001`。
